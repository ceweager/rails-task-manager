<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% unless f.object.new_record? %>
    <%= f.input :completed %>
  <% end %>
  <%= f.button :submit, class: "btn btn-primary" %><%= link_to 'home', tasks_path, class: "btn btn-outline-secondary" %>
<% end %>